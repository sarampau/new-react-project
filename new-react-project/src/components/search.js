import React from 'react';

const Search = (props) => {
    return (
        <div>
            <input placeholder='Search city here..'/>
            <button>Search</button>
        </div>
    )
}

export default Search;